<div class="alert alert-danger">
  <div>
    <div class="font-medium">Conflicting options</div>
    <ul class="mt-1 list-disc list-inside text-sm">
      {[ckeyword]<li><a href="#{ckeyword}">{ckeytext}</a>: {cchoice}</li>}
    </ul>
    <p class="mt-1 text-xs">Change one or more of these options to resolve the conflicts.</p>
  </div>
</div>
