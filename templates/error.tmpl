<div class="alert alert-danger">
  <div>
    <div class="font-medium">{?title} {?printer_name} Error</div>
    <p class="mt-1 text-sm opacity-90">{?message?{message}:Error}</p>
    <pre class="mt-2 text-xs whitespace-pre-wrap font-mono opacity-80">{error}</pre>
  </div>
</div>
